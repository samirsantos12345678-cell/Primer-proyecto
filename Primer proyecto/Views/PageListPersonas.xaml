<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Primer_proyecto.Views.PageListPersonas"
             Title="Lista de Personas">
    <StackLayout>
        <ListView x:Name="listaPersonas" ItemsSource="{Binding}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" Padding="10">
                            <StackLayout HorizontalOptions="StartAndExpand">
                                <Label Text="{Binding NombreCompleto}" FontSize="16" FontAttributes="Bold" />
                                <Label Text="{Binding Correo}" FontSize="14" />
                                <Label Text="{Binding Telefono}" FontSize="14" />
                            </StackLayout>
                            <StackLayout Orientation="Horizontal" HorizontalOptions="End">
                                <Button Text="Editar" CommandParameter="{Binding .}" Clicked="OnEditarClicked" />
                                <Button Text="Eliminar" CommandParameter="{Binding .}" Clicked="OnEliminarClicked" />
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Agregar Persona" Clicked="OnAgregarClicked" Margin="10" />
    </StackLayout>
</ContentPage>